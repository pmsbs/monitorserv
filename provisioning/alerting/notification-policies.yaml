apiVersion: 1
policies:
  - receiver: ti-alert
    group_by: ['alertname', 'instance']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: ti-alert
        matchers:
          - alertname != ""
